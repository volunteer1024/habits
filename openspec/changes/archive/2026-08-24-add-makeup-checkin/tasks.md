## 1. 窗口与流水类型

- [x] 1.1 增加「最近三天窗口」判断（`today-2 <= date <= today`）及页头称呼（今天 / 昨天 / 前天），用 Vitest 覆盖边界（含月初跨月）
- [x] 1.2 在积分流水类型中增加 `monthly_bonus_undo`；统计按任务筛选时同时识别 `monthly_bonus` 与 `monthly_bonus_undo`

## 2. 按业务日写入任务与坏习惯

- [x] 2.1 将 `getTodayItems` 收成 `getItemsForDate(date)`，缺实例才写入，并用 Vitest 确认连续读取不重复写 store
- [x] 2.2 `complete` / `undo` 接受业务日，窗外拒绝；Vitest 覆盖补完成昨天、撤销前天、窗外拒绝、锁定积分仍按首次完成
- [x] 2.3 `record` / 删除坏习惯记录按记录业务日校验窗口；Vitest 覆盖补记昨天、删除前天、窗外仍 `HISTORY_LOCKED`；把「隔天不可删」旧用例改成窗外日期
- [x] 2.4 Vitest：今日兑奖后补记昨天坏习惯，兑换保留且余额为负

## 3. 已结算月全勤对账

- [x] 3.1 任务完成 / 撤销后，若该日所在月已结算，按设计对账并追加 `monthly_bonus` 或 `monthly_bonus_undo`，更新 `awardedTaskIds`
- [x] 3.2 Vitest：缺一天结算后补齐则补发；全勤结算后撤销则追回且旧流水保留；结论未变不追加；本月补齐不发奖；改坏习惯不触发对账
- [x] 3.3 确认既有懒结算用例（跨月发一次、月中建任务无奖、重复打开不重发）仍然通过

## 4. 打卡页与导航

- [x] 4.1 主导航「今日」改为「打卡」，路由仍为 `/today`
- [x] 4.2 打卡页以本地 state 保存选中日；页头显示今天 / 昨天 / 前天，右侧 `ChevronsUpDown`；列表与坏习惯绑定选中日
- [x] 4.3 弹出月历（现有 Dialog / Drawer + `calendarGrid`）：仅三天可点，禁止未来月，窗口跨月可翻到上个月
- [x] 4.4 选中日掉出窗口时在 `useEffect` 拨回今天，不在 render 里写 state
- [x] 4.5 完成 / 撤销 / 记坏习惯 / 删除都传入选中日；空状态文案随是否今天区分

## 5. 回归

- [x] 5.1 跑完整 Vitest；用浏览器走：默认今天、切昨天补任务、补/删坏习惯、月历禁用窗外日期、积分页流水业务日正确
